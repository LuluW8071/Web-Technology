// Greet function
function greet(name, lname) {
    console.log('Hello ' + name + ' ' + lname);

}

// Calculate square of a number
function square(n) {
    return n * n;
}

// let n = square(2);
console.log(square(2));

greet('Programmer', 'Smith');