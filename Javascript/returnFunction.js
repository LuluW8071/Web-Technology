// Return function
function math(num) {
    return num * 9;
}

console.log(math(7));

// Assignment with a returned value
function change(sum) {
    return (sum + 10) / 2;
}

changed = change(100);
console.log(changed);