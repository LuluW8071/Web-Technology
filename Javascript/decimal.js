var decimal = 0.9;
console.log(decimal);

var product = 2.0 * 2.3;
var quotient = 2.5 / 0.5;
var remainder = 11 % 3;

console.log('Product:',product, 
            'Quotient:',quotient, 
            'Remainder:',remainder);